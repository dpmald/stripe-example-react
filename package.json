{
  "name": "feeding-kittens",
  "version": "1.0.0",
  "description": "Example using Stripe Payment Intents API",
  "license": "MIT",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
    "dev:server": "cd client && yarn build && cd .. && yarn start",
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "yarn": "^1.22.4"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
